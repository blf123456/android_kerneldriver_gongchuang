# 安卓内核驱动

> 一个强大的安卓内核驱动，支持 Linux 内核 5.1 至 6.12（Android 12-16），通过自定义网络协议族提供高级内存操作能力。

## 📑 目录

- [功能特性](#-功能特性)
- [技术架构](#-技术架构)
- [环境要求](#-环境要求)
- [快速开始](#-快速开始)
- [API 文档](#-api-文档)
- [工作原理](#-工作原理)
- [性能指标](#-性能指标)
- [兼容性](#-兼容性)
- [待实现功能](#-待实现功能)
- [安全声明](#-安全声明)
- [致谢](#-致谢)

## 🌟 功能特性

### 核心能力

| 功能 | 描述 | 技术实现 | 状态 |
|------|------|---------|------|
| **PTE 物理内存读写** | 直接访问任意物理内存地址 | 手动页表遍历 + PTE 重映射 | ✅ 已实现 |
| **PTE线性 物理内存读写** | 直接访问任意物理内存地址 | 手动页表遍历 + PTE 映射 | ✅ 已实现 |
| **进程内存读写** | 跨进程读写目标进程内存 | 使用 access_process_vm | ✅ 已实现 |
| **模块基址解析** | 获取目标进程已加载模块的基址 | VMA 遍历 + 文件路径匹配 | ✅ 已实现 |
| **进程发现** | 通过包名/进程名查找 PID | 进程遍历 + 名称匹配 | ✅ 已实现 |
| **自定义协议族** | 用户态与内核态安全通信 | 动态注册 socket 协议族 | ✅ 已实现 |
| **驱动隐藏** | 从系统各处隐藏驱动痕迹 | 链表删除 + kobject 移除 | ✅ 已实现 |
| **硬件 MMU 翻译** | 使用 AT 指令进行地址翻译 | ARM64 AT S1E0R 指令 | ❌ 未实现 |
| **触摸设备模拟** | 模拟触摸输入事件 | uinput/input 子系统 | ❌ 未实现 |
| 驱动自隐藏 | 从系统各处隐藏驱动痕迹 | 链表删除 + kobject 移除 | ✅ 已实现 |
### 技术亮点

- ✅ **多版本兼容**：支持 Linux 内核 5.1 至 6.12
- ✅ **ARM64 优化**：使用 `TLBI VAALE1IS` 指令硬件加速 TLB 刷新
- ✅ **完整页表遍历**：PGD → P4D → PUD → PMD → PTE
- ✅ **大页检测**：自动识别 1GB 和 2MB 大页
- ✅ **软件 TLB 缓存**：优化地址翻译性能
- ✅ **Normal-NC 映射**：缓存一致性的物理内存访问
## 🔧 技术架构

```
┌─────────────────────────────────────────────────┐
│              用户空间应用程序                      │
│            (需要 Root 权限)                       │
└──────────────────┬──────────────────────────────┘
                   │ Socket 通信
                   │ (自定义协议族)
┌──────────────────▼──────────────────────────────┐
│                   内核驱动                        │
│                                                   │
│  ┌───────────────────────────────────────────┐  │
│  │         Socket 接口层                      │  │
│  │  ┌─────────────┐  ┌──────────────────┐   │  │
│  │  │ getsockopt  │  │   setsockopt     │   │  │
│  │  └─────────────┘  └──────────────────┘   │  │
│  └──────────────────┬───────────────────────┘  │
│                     │                           │
│  ┌──────────────────▼───────────────────────┐  │
│  │          内存管理模块                      │  │
│  │  ┌──────────┐ ┌──────────┐ ┌─────────┐  │  │
│  │  │ PTE 映射 │ │ 页表遍历  │ │TLB 管理 │  │  │
│  │  └──────────┘ └──────────┘ └─────────┘  │  │
│  └──────────────────┬───────────────────────┘  │
│                     │                           │
│  ┌──────────────────▼───────────────────────┐  │
│  │          进程管理模块                      │  │
│  │  ┌──────────┐ ┌──────────┐ ┌─────────┐  │  │
│  │  │ PID 解析 │ │ 模块基址  │ │VMA 遍历 │  │  │
│  │  └──────────┘ └──────────┘ └─────────┘  │  │
│  └───────────────────────────────────────────┘  │
│                                                   │
│  ┌───────────────────────────────────────────┐  │
│  │          隐藏模块                           │  │
│  │  /proc/modules  /sys/module  /proc/vmalloc│  │
│  └───────────────────────────────────────────┘  │
└───────────────────────────────────────────────────┘
```

## 📋 环境要求

| 项目 | 要求 |
|------|------|
| **内核版本** | Linux 5.1 - 6.12 |
| **架构** | ARM64 (ARMv8-A) |
| **编译器** | GCC 或 Clang (交叉编译) |
| **权限** | Root 权限 |
| **安卓版本** | Android 12 - 16 |

## 🚀 快速开始

### GitHub Actions 云编译（DDK）

打开仓库的 [Actions 页面](https://github.com/blf123456/android_kerneldriver_gongchuang/actions/workflows/ddk-build.yml)，选择 **Android kernel modules (DDK)** → **Run workflow** 即可开始。修改驱动源码、DDK 构建脚本或工作流后推送，也会自动编译；Pull Request 同样会触发检查。

使用与 `blf123456/DFM_kernel_UC` 相同的 Ylarod DDK 镜像，固定版本为 `20260313`，每次独立编译以下六个 ARM64 目标：

| Android | 内核 | 下载产物名称 |
|---------|------|--------------|
| 12 | 5.10 | `gongchuang-android12-5.10` |
| 13 | 5.10 | `gongchuang-android13-5.10` |
| 13 | 5.15 | `gongchuang-android13-5.15` |
| 14 | 6.1 | `gongchuang-android14-6.1` |
| 15 | 6.6 | `gongchuang-android15-6.6` |
| 16 | 6.12 | `gongchuang-android16-6.12` |

编译成功后，在该次运行底部的 **Artifacts** 下载对应压缩包，内含 `gongchuang.ko`、SHA-256 校验值、模块信息、源码提交号、镜像摘要和编译日志，保留 30 天。失败任务另存诊断日志，保留 14 天。无需额外配置 Secrets。

构建在临时源码副本中进行，不会把仓库已有的 `.ko` 当作新产物。默认 `HIDE_SELF_MODULE=0` 现在同时控制隐藏函数的编译和调用，模块保持可见；产物保留调试符号和 DDK 提供的符号版本信息。云编译只验证生成 ARM64 模块，不代表已通过目标设备加载测试；实际加载需要匹配设备的内核配置、ABI 和签名要求。

在安装了 Docker 的 Linux / WSL 中复现单个目标：

```bash
git clone https://github.com/blf123456/android_kerneldriver_gongchuang.git
cd android_kerneldriver_gongchuang
export DDK_TARGET=android14-6.1
export DDK_IMAGE="ghcr.io/ylarod/ddk:${DDK_TARGET}-20260313"
docker run --rm --platform linux/amd64 \
  -e DDK_TARGET -e DDK_IMAGE \
  -v "$PWD:/build" -w /build \
  "$DDK_IMAGE" bash -lc 'bash scripts/build-ddk.sh'
ls "artifacts/$DDK_TARGET/"
```

已经配置 DDK 工具链的 Linux 主机也可以执行 `DDK_TARGET=android14-6.1 make -j"$(nproc)"`，生成 `src/gongchuang.ko`。内核构建目录可通过 `KDIR=/path/to/kernel/output` 覆盖。

### 编译

```bash
# 1. 克隆项目
git clone https://github.com/blf123456/android_kerneldriver_gongchuang.git
cd android_kerneldriver_gongchuang

# 2. 配置编译脚本
# 编辑 build_all.sh，设置以下变量：
# - KERNELS_ROOT: 各版本内核源码的父目录
# - DRIVER_SRC: 本项目 src 目录的绝对路径

# 3. 运行编译脚本
./build_all.sh

# 4. 查看生成的内核模块
ls -la *.ko
```

### 安装

```bash
# 1. 推送到设备
adb push install_driver.sh /data/local/tmp/

# 2. 获取 root 权限
adb shell
su

# 3. 加载模块
cd /data/local/tmp/
./install_driver.sh
```

## 📖 API 文档

### 操作码列表

| 操作码 | 功能 | 参数 | 说明 |
|--------|------|------|------|
| `GET_PROCESS_PID` | 通过进程名获取 PID | 进程名 | 返回 PID |
| `ATTACH_PROCESS` | 附加到目标进程 | PID | 必须先附加 |
| `IS_PROCESS_PID_ALIVE` | 检查进程是否存活 | PID | 返回 1/0 |
| `GET_MODULE_BASE` | 获取模块基址 | 模块名 | 返回基址 |
| `ACCESS_PROCESS_VM` | 普通跨进程读写 | 地址和大小 | 使用内核 API |
| `PTE_PHYS_READ_MEMORY` | PTE 方式读取内存 | 虚拟地址 | 手动页表遍历 |
| `PTE_PHYS_WRITE_MEMORY` | PTE 方式写入内存 | 虚拟地址 | 手动页表遍历 |

### 使用示例

> 📝 用户空间使用示例暂未完善，欢迎贡献！

## 🔬 工作原理

### PTE 内存读写流程

```
1. 用户空间发起请求
   ↓
2. Socket 通信传送到内核
   ↓
3. 获取目标进程 mm_struct
   ↓
4. 手动遍历页表 (PGD → P4D → PUD → PMD → PTE)
   ↓
5. 获取物理地址
   ↓
6. 修改 PTE 映射到目标物理页
   ↓
7. 刷新 TLB
   ↓
8. 直接读写映射后的虚拟地址
   ↓
9. 返回结果给用户空间
```

### 页表遍历详解

```
虚拟地址 (48-bit)
┌─────┬─────┬─────┬─────┬─────┬────────┐
│ PGD │ P4D │ PUD │ PMD │ PTE │ Offset │
│ 9bit│ 9bit│ 9bit│ 9bit│ 9bit│ 12bit  │
└─────┴─────┴─────┴─────┴─────┴────────┘
  ↓     ↓     ↓     ↓     ↓      ↓
 512   512   512   512   512    4KB
entries entries entries entries entries page
```

## 📊 性能指标

| 指标 | 数值 | 说明 |
|------|------|------|
| 地址翻译 | O(1) | 带软件 TLB 缓存 |
| 内存访问延迟 | ~100ns | 直接 PTE 映射 |
| TLB 刷新 | ~1μs | 硬件加速广播 |
| 页表遍历 | ~500ns | 4 级页表 |
| 跨页读写 | 自动处理 | 逐页循环 |

## 🔄 兼容性矩阵

| 内核版本 | Android 版本 | 状态 |
|---------|-------------|------|
| 6.12 | Android 16 | ✅ 支持 |
| 6.6 | Android 15 | ✅ 支持 |
| 6.1 | Android 14 | ✅ 支持 |
| 5.15 | Android 13 | ✅ 支持 |
| 5.10 | Android 13 | ✅ 支持 |
| 5.10 | Android 12 | ✅ 支持 |

## 🚧 待实现功能

### 硬件 MMU 翻译
- [ ✅ ] 使用 ARM64 `AT S1E0R` 指令进行地址翻译
- [ ✅ ] 处理翻译失败的 SEA（Synchronous External Abort）
- [✅  ] 优化翻译性能

### 触摸设备模拟
- [ ] 实现虚拟触摸设备
- [ ] 支持多点触摸协议（Type B）
- [ ] 模拟滑动、点击等手势
<<<<<<< HEAD
=======
      
- [ ] cfi绕过
>>>>>>> bab7d981866ee26532c9b27d41280b64bdc581b7

## 🔒 安全声明

### ⚠️ 警告

此驱动提供底层内存访问能力，可能：

- **绕过安全机制**：绕过 SELinux、Seccomp 等
- **导致系统不稳定**：错误的内存操作可能导致内核崩溃
- **被恶意利用**：可能被用于开发恶意软件

### ✅ 合法用途

- 📚 教育学习
- 🔬 安全研究
- 🐛 授权调试
- 🛡️ 渗透测试（受控环境）

### ❌ 禁止用途

- 🚫 开发恶意软件
- 🚫 未授权访问
- 🚫 商业用途（未经许可）

## 🙏 致谢

特别感谢以下项目和作者：

- **[Linux-android-arm64](https://github.com/lsnbm/Linux-android-arm64/tree/main)** - 提供了重要的技术参考和支持

> 我只是站在巨人的肩膀上，感谢原作者的开源精神！

## 📝 许可证

本项目仅供**教育目的**使用，使用风险自负。

## 🤝 贡献指南

欢迎贡献！请确保：

1. 代码遵循 Linux 内核编码规范
2. 在多个内核版本上测试
3. 更新相关文档
4. 添加适当的注释
5. 提交前运行 `checkpatch.pl`

## 📧 联系方式

如有问题或建议，请提交 Issue 或 Pull Request。

---

**⭐ 如果这个项目对你有帮助，请给个 Star！**

**🔗 感谢项目：[Linux-android-arm64](https://github.com/lsnbm/Linux-android-arm64/tree/main)**
